# Codex weekly automation

Recommended schedule: every Monday at 06:00 in Asia/Tokyo.

Prompt:

> In the saved AI Opportunity Monitor project, execute `npm run doctor`, then use the repository skill `update-ai-opportunity-monitor` to refresh the board. Use the signed-in Codex subscription. Do not use OpenAI or Anthropic API keys. Prioritize official sources, use AIhot and PR TIMES only for discovery, and report confirmed changes, review candidates, source gaps, and the three most important decision points.

For one shared board, designate one account as the updater. Other team members can clone the same repository without creating duplicate weekly automations.
